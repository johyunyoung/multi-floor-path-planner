go2_0000:
  ekf_node:
    ros__parameters:
      odom_frame: 'odom'
      base_link_frame: 'base_link'
      world_frame: 'odom'
      publish_tf: true
      two_d_mode: false
      frequency: 50.0
      use_sim_time: true
      odom0: 'ground_truth/odom'
      odom0_config: [true, true, true,
                     true, true, true,
                     true, true, true,
                     false, false, true,
                     false, false, false]
      odom0_differential: false
      odom0_queue_size: 10
